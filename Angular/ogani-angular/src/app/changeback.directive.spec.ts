import { ChangebackDirective } from './changeback.directive';

describe('ChangebackDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangebackDirective();
    expect(directive).toBeTruthy();
  });
});
